#!/bin/sh
#
# Description: synchronize IMAP accounts to Maildir in gocryptfs vault
# Author: Victor Laborie
# License: MIT
#

set -eu

~/.local/bin/unlock-gpg-agent || {
    [ "${?}" -eq 2 ] && exit 0
}
~/.local/bin/open-encrypted-maildir
/usr/bin/mbsync --all
